[project]
name = "Rate-Limiter"
version = "0.1.0"
description = "FastAPI-based rate limiter using Redis."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.135.3",
    "redis[hiredis]>=7.4.0",
]
